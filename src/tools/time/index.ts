// Time tool bundle. Single tool today (`time_now`); kept in its own
// directory so the module shape matches memory/ and dream/ for
// consistent registration.

export { timeNow, timeTools } from './tools.ts';
